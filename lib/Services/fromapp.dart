import 'package:flutter/material.dart';

class Fromapp extends StatelessWidget {
  const Fromapp({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Colors.white,
        title: Text("عن التطبيق"),
      ),
    );
  }
}
